for creating requirements.txt
pip freeze > requirements.txt
Ejecutar el comando correspondiente (pip install -r requirements.txt, npm install, composer install, etc)
ej. pip install -r requirements.txt
